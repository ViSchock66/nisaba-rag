# Runtime dependencies.
#
# This file mirrors [project].dependencies in pyproject.toml for people who
# prefer `pip install -r`. Keep both in sync when adding a dependency.
#
# The reranker stack is listed here on purpose: hybrid search with reranking is
# the headline feature, so a default install must include it.

# ── Core engine ───────────────────────────────────────────────────────
chromadb>=0.5.0
# v2 renamed FastMCP to MCPServer and dropped mcp.server.fastmcp; server.py
# imports the v2 path, so v1 is excluded rather than merely discouraged.
mcp>=2.0.0
httpx>=0.27.0

# ── Reranker (cross-encoder, ONNX Runtime, CPU) ───────────────────────
onnxruntime>=1.17.0
tokenizers>=0.15.0
numpy>=1.26.0

# ── Optional GPU providers ────────────────────────────────────────────
# Not installed by default because each one is hardware-specific. Install the
# one that matches your machine and set reranker.provider accordingly
# ("cuda" or "dml"); the engine falls back to CPU when it is missing.
#
#   NVIDIA:  onnxruntime-gpu>=1.17.0
#   Windows: onnxruntime-directml>=1.17.0
